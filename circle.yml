machine:
  timezone: America/Los_Angeles

dependencies:
    override:
#        - echo y | android update sdk --no-ui --all --filter tools,platform-tools,build-tools-26.1.0,android-25,extra-google-m2repository,extra-google-google_play_services,extra-android-support
        - brew install Caskroom/cask/android-sdk
        - sudo ANDROID_HOME=/usr/local/android-sdk-linux ./gradlew dependencies

#test:
#  override:
#    - ./gradlew clean
#    - ./gradlew assembleDebug
#    - ./gradlew test